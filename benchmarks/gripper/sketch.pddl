(:sketch gripper
  (:memory_states (m0 m1 m2))
  (:registers (r0 r1 r2))
  (:booleans )
  (:numericals )
  (:concepts )
  (:rule (:conditions (:memory )) (:effects (:memory )))
  (:initial_memory m0)
)
